/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // Quét các file trong thư mục src để áp dụng Tailwind CSS
    ],
    theme: {
      extend: {
        // Tùy chỉnh theme tại đây (nếu cần)
      },
    },
    plugins: [
      // Thêm plugin của Tailwind CSS tại đây (nếu cần)
    ],
  };